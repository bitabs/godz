import { colors as common } from '../common/core.tokens.colors'

const text = {
  'text--primary--light': '$white',
  'text--primary--main': '$white',
  'text--primary--medium': '$sla400',
  'text--primary--dark': '$sla600',
}

export const colors = {
  ...common,
  ...text,
}
